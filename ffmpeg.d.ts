// declare module '@ffmpeg/ffmpeg' {
//   export type FFmpeg = {
//     load: () => Promise<void>;
//     run: (...args: string[]) => Promise<void>;
//     FS: (command: 'writeFile' | 'readFile', filename: string, data?: Uint8Array) => Uint8Array | void;
//   };

//   // Remove fetchFile from here, as it's not a method
// }
